<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="loginAccount">
		SELECT
			   ACCOUNT_ID
			 , PASSWORD
			 , NAME
			 , EMAIL
			 , PHONE
			 , AUTH
			 , STATUS
		  FROM
		       ACCOUNT
		 WHERE
		       ACCOUNT_ID = ?
		   AND PASSWORD = ?
		   AND STATUS = 'Y'
	</entry>
</properties>